"""Datasets for spark_data_analysis

This module contains dataset definitions for the spark_data_analysis package.
Each table is defined as an Enum to make it easier to reference the columns
in the code. The schema is also defined as a StructType to make it easier to
load the dataset into a DataFrame using Spark.
"""
